#include <bits/stdc++.h>
#include <cstring>
using namespace std;

int main() {
    int K[5];
    memset(K, 2, sizeof(K));
    for (int i=0; i<5; i++){
        cout << K[i] << " ";
    }
    int G[5];
    memset(G, 2, sizeof(K));
    for (int i=0; i<5; i++){
        cout << G[i] << " ";
    }
    return 0;
}
